{
  "scripts": {
    "build": "nuxt build",
    "prestart": "npm run build",
    "start": "cross-env NODE_ENV=production node index",
    "watch": "node index"
  },
  "dependencies": {
    "cross-env": "^5.1.3",
    "nuxt": "^1.3.0",
    "polka": "^0.3.4"
  }
}
