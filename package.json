{
  "private": true,
  "author": {
    "name": "Luke Edwards",
    "email": "luke@lukeed.com",
    "url": "https://lukeed.com"
  },
  "scripts": {
    "pretest": "lerna bootstrap",
    "prepublish": "cp readme.md packages/polka/.",
    "test": "tape tests/*.js | tap-spec",
    "lint": "biome lint bench/*.js && biome lint packages/**/*.js",
    "lint:fix": "biome lint bench/*.js --apply-unsafe && biome lint packages/**/*.js --apply-unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "axios": "0.19.0",
    "lerna": "2.8.0",
    "tap-spec": "4.1.1",
    "tape": "4.8.0"
  }
}
