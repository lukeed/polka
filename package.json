{
  "private": true,
  "author": {
    "name": "Luke Edwards",
    "email": "luke@lukeed.com",
    "url": "https://lukeed.com"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "lint": "eslint packages --env node,es6",
    "pretest": "npm run lint && npm run bootstrap",
    "test": "tape packages/*/test/*.js | tap-spec"
  },
  "devDependencies": {
    "eslint": "5.15.0",
    "httpie": "1.1.0",
    "lerna": "3.13.4",
    "tap-spec": "5.0.0",
    "tape": "4.10.1"
  },
  "eslintConfig": {
    "extends": "eslint:recommended",
    "parserOptions": {
      "ecmaVersion": 8
    },
    "overrides": [
      {
        "files": [
          "packages/*/test/**/*.js"
        ],
        "rules": {
          "no-console": 0
        }
      }
    ]
  },
  "eslintIgnore": [
    "**/node_modules/**"
  ]
}
